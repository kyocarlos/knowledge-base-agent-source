# AGENTS.md

全域工作規則：

1. 每當要壓縮對話時，同時也更新該專案的進度記憶。
2. 每次開啟專案時，先讀取專案記憶，再開始後續工作，避免對話每次都從零開始。
3. 專案記憶檔案使用 `PROJECT_MEMORY.md`。
4. 每次產生最後方案後，將 LLM 的說明與結論完整寫入專案記憶，方便下次直接接續。
5. 無論進行任何修改，預設都要避免硬編碼，優先採用可擴充、可配置、資料驅動或共用規則的做法；只有在使用者明確指定要硬編碼時，才可採用硬編碼方案。

## 網頁測試原則

當需要測試或驗證網頁功能、網頁設計、或前端修改結果時，請優先使用 Webwright 進行真實使用者流程測試；只有在 Webwright 無法完成、遇到工具限制、或需要直接排查瀏覽器細節時，才改用 Playwright 或其他可用的瀏覽器自動化工具。

測試時請遵守：

1. 優先實際開啟網頁，而不是只閱讀程式碼推測結果。
2. 使用真實使用者流程測試，例如：
   - 點擊按鈕
   - 輸入表單
   - 選擇下拉選單
   - 上傳檔案
   - 切換頁面
   - 檢查錯誤訊息
3. 不要只用 API 呼叫取代 UI 測試，除非任務明確要求。
4. 測試登入流程時，請使用使用者提供的測試帳號，不要自行猜測正式帳號密碼。
5. 若畫面異常，請截圖保存並說明：
   - 發生在哪個頁面
   - 操作步驟
   - 預期結果
   - 實際結果
6. 若有 console error、network error 或 failed request，請一併檢查。
7. 若發現 bug，請盡可能產生可重現步驟。

---

## Webwright / Playwright 測試規範

如果專案需要 E2E 測試，請優先使用 Webwright；Playwright 只作為備援工具，適用於 Webwright 無法處理、需更細緻的瀏覽器除錯、或既有測試流程已明確依賴 Playwright 的情況。
5. 任何修改與修正都必須先評估不同電腦、不同 session、不同 runtime 與不同部署路徑的行為差異，不能只針對單一機器或單一現象修補。
6. 解決方案必須覆蓋所有合理可預見的情況與失敗模式，優先處理根因與共通機制，避免只修掉當下看到的表面症狀。
7. 若目前方案只能解決局部案例，必須明確標註其適用範圍與未覆蓋風險，並補上驗證計畫，確認不會在其他環境或流程中回歸。
8. 每次修正一個問題後，必須先自行完成驗證，確認行為正常、沒有回歸，才能對使用者回報已修正。
9. 每次修改前，必須先回顧既有修改與已修正內容，確認新修改不會影響已修正的行為，再開始處理新的問題。
10. 往後知識庫相關的測試與修改，優先以 `https://61.216.9.52:3030/chat.html` 作為主要驗證入口；除非任務明確要求，否則不要改用其他入口作為唯一依據。
11. 每次進行修改時，必須先確認前一次及既有修正內容，再開始執行新的修改；任何新改動都不得影響前幾次已完成的修正，若有衝突要先調整整體方案再動手。

## Claude Code 十二條規則

以下規則取自 Bnext 文章中整理的 12 條 Claude Code 行為約束，作為專案層補充：

1. 編碼前先思考，先說明假設、權衡與不確定點，不要默默下結論。
2. 簡單優先，用最少代碼解決問題，不加入想像中的功能或過度抽象。
3. 外科手術式修改，只改必要部分，不要順手重構無關區塊。
4. 以目標為導向執行，先定義成功標準，再迭代到驗證通過。
5. 不要讓模型做確定性工作，路由、重試、狀態判斷等交給普通程式碼。
6. 設定硬性 token 預算，接近上限就總結狀態並重啟，不要硬撐。
7. 遇到互相矛盾的現有模式，不要折中混合，應選定一種並說明理由。
8. 先讀再寫，新增程式前先看導出、呼叫方與共享工具。
9. 測試不是可選，但測試本身不是目標；測試要能驗證真正的業務行為。
10. 長時間多步驟任務要設檢查點，每一步都要能說清已做、已驗證、待完成事項。
11. 約定優先於個人新意，既有命名、架構、風格與模式優先維持一致。
12. 要顯性失敗，不要靜默失敗；不確定成功就明說，不要包裝成完成。
